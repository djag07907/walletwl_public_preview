// rules_version = '2';

// service cloud.firestore {
//   match /databases/{database}/documents {
//     // Allows anyone with your API key to read/write.
//     // Use this ONLY for initial setup/development with Mock Users.
//     match /{document=**} {
//       allow read, write: if true;
//     }
//   }
// }

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // secure version for production
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}